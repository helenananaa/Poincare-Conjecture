# Lean / Lake build artifacts
.lake/
build/
*.olean
*.ilean
*.trace

# Blueprint / web build output
_site/
**/dashboard.html
**/blueprint/build/
**/blueprint/print/
**/blueprint/web/
**/blueprint/**/*-check.*
**/blueprint/**/testmain.tex
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.ilg
*.ind
*.idx
*.log
*.nav
*.out
*.paux
*.pdf
*.pdf.bak
*.run.xml
*.snm
*.synctex.gz
*.toc
*.vrb
*.xdv

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Agent / workspace tooling (not part of the clean repo)
/.agents/
/.codex/
.archon-horizon/
.git.disabled/

# hgraph source-derived records are regenerated in CI. Nested attachment files
# such as nodes/<id>/review-1.md and comment-1.md remain trackable.
**/hgraph/nodes/*.md
**/hgraph/edges/*.md

# Static site output is built and deployed by GitHub Actions.
/docs/

# Working notes and drafts. These live in the Horizon workspace this repo is
# synced from; the published copy carries only finished material.
**/PLAN.md
**/PLAN_*.md
*.lean.wip
test/
site/proof-map/dist/
site/proof-map/node_modules/

# Local parallel-agent state and authentication must never be committed.
tools/lean_swarm/__pycache__/
*.local.json
.swarm-runtime/
