# 证明加速计划与本轮验收

目标是缩短完整证明的依赖链，而不是最大化代理数量、代码行数或被删除的 sorry 数。

## 两条工作线

**庞加莱主线**：本次源码审计确认，主入口是导入聚合器，尚无无条件的最终 Lean 定理。现有拓扑、变分和比较估计不能代替 Ricci 流存在性、受控手术、有限时间灭绝与拓扑重建。当前主入口的项目依赖闭包没有 LeeSmooth 导入，因此 Lee 全库清零不是这条主线已完成的证据。

**Lee 库收尾**：继续完成原有任务和调用接口修复。原始证明洞计数与数学任务完成情况分开记录；保留旧陈述和明确的适用范围，不靠删除命题或添加目标等价假设减少数字。

## 本轮已执行

| 项目 | 状态 | 证据 |
|---|---|---|
| Exercise 4.9 | 原半空间目标结论已恢复，无不可能的全局 Boundaryless 假设；已集成 | 独立源码构建、生产构建、8 个声明的传递公理检查通过 |
| SardMoreira 移植 | 27 个模块已独立重建并集成，欧氏空间版本的 Sard 为无条件结果 | 5 个关键声明的独立及生产公理检查通过 |
| Sard 旧通用接口 | 没有替换成带额外延拓假设的声明 | 原 strict core 及其调用约定保持原样 |
| 主线依赖审计 | Luna high 已执行源码审计，产出候选路线 | l13-PoincareCriticalPath/ROADMAP.md |
| 标准模型 Sard 连接 | Luna xhigh 已启动 | l14-SardStandardModels |
| 一般三维环境模型的原生颈闭包适配 | Luna xhigh 已启动；候选仍在隔离目录 | l15-NativeNeckAmbientAdapter |

本轮新增验收覆盖 13 个声明，不等于减少了 13 个原始证明洞。生产源码仍有 6 个显式证明洞、分布于 4 个文件。上一轮 8 个失败模块的定向复检，已有 Exercise 4.9 通过，仍有 7 个失败；不声称整个 Lee 库已经构建干净。

## 当前优先顺序

1. 让已完成的基础定理可被真实调用：Sard 的标准半空间/流形版本和原生颈的一般环境模型适配并行推进。
2. 修复已有消费者的边界、正则性和图册接口，保持原有效结论；优先处理能解除下游阻塞的证明。
3. 带边界常秩继续证明正确的一般分支；幂零 Ado 继续具体的加权 PBW 重排。两者使用 max，不重复派人证明已经验收的 PBW。
4. 一般 Ado 的宽泛任务不自动重开；需要新的已验收前提或明确独立的子问题。它仍列在 Lee 待办中，不充当庞加莱主线的串行前置条件。

## 验收标准

每个成果依次检查：陈述是否仍是要证明的命题；源码是否在固定 Lean 4.32.1 下编译；传递公理是否只有 propext、Classical.choice、Quot.sound；真实调用方是否编译。已有消费者的失败不被源码局部清零掩盖。

Sard 的下一验收点是：从已证明的欧氏 Sard 和 Seeley 延拓，得到标准无边界/半空间模型上的可用结果，最终声明不额外假设延拓存在。无限阶光滑的局部量词不能随意交换；允许先使用足够高有限阶 Sard 与有限阶延拓，再接流形全局光滑数据。

主线适配的下一验收点是：只从三维有限维、原有无边界模型和已有原生颈 cut/frontier 假设，构造坐标变换并返回原模型下的平滑闭包、嵌入及精确边界对应。不得把所需模型兼容性重新放进假设。

幂零 Ado 的下一验收点是：真正证明 HighWeightOrderedRewrite，而不是将它继续作为新参数。一般 Ado 的非分裂/更高幂零阶部分仍是独立未完成义务。

已对路线审计的建议进行筛选：将已有 displayed-union 定理沿一个假设的 frontier 等式改写，不值得再开模型；已有光滑球丛分支也不重新派工。真正需要几何提供的 cut/frontier、手术或流对象仍明确列为待证明条件。

## 模型与资源

只调用 gpt-5.6-luna，显式指定 high、xhigh 或 max。Grok、SOL 及低于 high 的档位均未使用。不同工人只编辑自己的隔离副本；已验收源码尽早供后续任务使用，不复制并信任旧编译产物。局部 Lean 编译继续受资源限流约束，没有清缓存或全量重建。

## 仓库与证据

分支仍为 integration，HEAD 仍为 e5217db。没有提交、推送或上游 PR。庞加莱与 MorganTian 保护范围内 908 个 Lean 文件哈希未变；本轮主线新证明任务尚未改写这些文件。

可重复执行的新增公理验收：

```bash
cd formalized-sources/LeeSmooth
lake env lean tests/BoundaryLocalDiffeomorphAudit20260920.lean
lake env lean tests/SardMoreiraPortAudit20260920.lean
```

本轮操作、源码快照、独立构建与生产日志位于：
`/home/helenanana/projects/poincare-ops-20260919/luna-handoff-20260920-180603/critical-path-20260920`。

这份计划不是完成证明的声明，也不是完成日期预测。最终验收仍必须是目标定理及其全部真实依赖通过 Lean 检查，且没有证明洞或被隐藏的数学假设。
