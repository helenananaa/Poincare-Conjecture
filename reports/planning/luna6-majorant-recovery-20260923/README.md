# Recovery of the shifted-third-derivative bottleneck — 2026-09-23

The seven independent j6 roots integrated. The shifted third-derivative majorant timed out, blocking six dependent tasks. No new controller or native worker was running when this continuation began. The timeout and its attempt record are retained; no failed proof is promoted.

Four new mathematical leaves are dispatched in parallel: a pointwise arbitrary-direction third-derivative bound; an exactly scaled Gaussian moment; the real-power polynomial identity with coefficients 2,4,3,1; and a generic interval-integrated min-modulus estimate. Three feed the original majorant. The fourth lets the time-integration subproblem proceed independently of the kernel-bound chain.

Seven k6 routes replace seven old j6 routes, preserving target paths, theorem names and exact declarations, with only explicit added imports and dependency edges. These are NOT seven additional mathematical achievements. superseded-routes.json must be merged with earlier aliases for reporting. Old consumers remain blocked by the old timeout; never retry old aliases or run duplicate target writers.

The majorant can now be assembled from D(y)=96*exp(1)*(2*M_alpha+4*M_(alpha+1)+3*M_(alpha+2)+M_(alpha+3)), with M_beta=K_(2t)(y)*norm(y)^beta/t^((beta-alpha+3)/2). Each term has the same integral time exponent alpha/2-3/2. No final estimate is inserted as an assumption, no constant is allowed to depend on t, and no theorem conclusion is weakened.

All declaration-only preflights passed. Their temporary admitted objects remain isolated and never enter production paths or proof evidence. Root calls use exactly gpt-6-luna/xhigh; downstream high/xhigh/max only. The shared compiler/verifier limits are 6/2, model count unlimited, and one common Git lock. Actual four-native-client concurrency was sampled.

Additional default-mode source checking found j6-C2-holder-complete has an elaboration/typeclass issue, while pinned verification and standard-axiom audit pass. A proof-body-only optimization is tested separately; no repair is accepted without recompilation and current-source provenance.

Smoothing and geometric_trace remain unbound. Full spatial Schauder, parabolic time regularity, variable-coefficient and nonlinear existence, flow/surgery/extinction remain distinct. No task count is a Poincare percentage. No full rebuild, remote push, upstream PR or persistent watcher is part of this work.
