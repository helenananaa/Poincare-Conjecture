# Temporal Hessian estimates — 2026-09-23

Five new fixed goals: four mutually independent roots and one dependent pointwise envelope.
The roots are the actual Hessian time derivative, comparable-positive-time Gaussian bound,
integrability of an explicit temporal Gaussian envelope, and the temporal min-kernel integral.
Every declaration passed isolated syntax preflight before dispatch. cards.json is authoritative.
Admitted statement-only objects are never production dependencies or proof evidence.

The spatial majorant timeout was resolved by the preceding recovery batch. Its remaining
spatial Schauder chain is still served by the same live dispatcher. These temporal roots do
not depend on completion of that chain. They develop ingredients for Hessian time regularity,
not a completed temporal Schauder theorem. In particular the forcing is not silently assumed
Holder in time, and no full parabolic/quasilinear existence result is asserted.

One additional generic weighted time-variation card was not written: two unchanged write
requests were rejected as safety-state indeterminate. It is omitted from cards and counts;
no alternative tool or rephrased payload was used to perform that rejected operation.
The separate pointwise envelope is a different calculation, with an explicit bound of 64.

All jobs use gpt-6-luna, high/xhigh/max; four root service intervals overlap.
Shared compile/verifier slots remain 6/2 with one Git writer. No new dispatcher, provider fallback,
full dependency rebuild, remote push, upstream PR, or production cache-policy change.
V1 smoothing and geometric_trace remain unbound; no completion percentage is asserted.
