# GPT-6 Luna independent proof frontier — 2026-09-23

This batch contains 17 fixed-statement tasks. All 17 declarations and import graphs were typechecked in a separate **statement-only** workspace. Admitted preflight stubs and their object files are NOT accepted proofs and are never used by production verification. JSON cards are templates, not compiled proof modules.

## Mathematical dependency lines

1. Standard-basis connection expansion + generic trilinear extension → basis metric compatibility → arbitrary-vector metric compatibility → actual metric product rule.
2. Local coercivity + actual gauge differentiability + symmetric principal contraction → actual neighborhood lowering → DeTurck Lie principal split → actual DeTurck residual bound.
3. Actual initial Riemannian metric: preserve the previous partition-of-unity source checkpoint and repair the independently observed finite-support and constructor obligations. No smooth-structure existence is assumed proved by this task.
4. Euclidean chart coefficient smoothness + entrywise matrix/operator differentiation → actual geometric chart jets → reparametrized actual Christoffel coefficients → actual connection/frame transport. Distinct frames must be related explicitly, not silently identified.

## Scheduling and acceptance

- Exact worker model is `gpt-6-luna`, with `high`, `xhigh`, or `max`; no model fallback.
- Model concurrency is unlimited, with dependency/path locks determining admission. All six initially independent leaves were dispatched concurrently; two further independent chart-analysis leaves extend the pool.
- Six shared compiler slots, two verifier slots, one Git writer. A finite dispatcher drains this reviewed batch; it is not an unbounded automatic retry loop.
- Only fixed proof bodies may change. Production acceptance reconstructs frozen statements, independently compiles actual source, audits transitive axioms, and integrates one verified file at a time.
- Additional default-option rechecks are recorded separately. Neither a worker exit nor statement-only preflight is acceptance.
- Historical task outcomes and failed source checkpoints remain intact. The six accepted replacement routes in the reconciliation file have matching current source blobs and ancestor commits; do not rerun their obsolete predecessor IDs.

## Public boundary

The V1 files and exact public target are unchanged. `smoothing` and `geometric_trace` remain unbound. These tasks advance local ingredients; neither task counts nor a green contract guard are a completion percentage for Poincare.

Only local commits are authorized for this batch. No remote push or upstream pull request is made. Runtime logs and further acceptance snapshots remain in the local operations directory, not in mutable worker claims.
