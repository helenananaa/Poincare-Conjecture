# Intrinsic Ricci and variable coefficient continuation — 2026-09-23

## Frozen tasks
Nine new mathematical targets: frame inverse coordinate formula; MT-to-DoCarmo curvature coefficient sign/order; chart trace to the actual intrinsic Ricci-flow tensor; actual PDE-coordinate Ricci to intrinsic Ricci; Holder control of inverse-metric coefficients; freezing-smallness principal difference estimate; explicit inverse-symbol ellipticity; germ-preserving global C2 selfadjoint extension; and the actual inverse-metric heat Hessian commutator.

Six independent seeds were dispatched, with three dependent consumers queued. All nine declarations passed isolated syntax preflight, which is not proof evidence. Preflight objects never enter production Lean paths.

## Recovered route
The failed c6-lowered-gauge-smooth candidate omitted an explicit task dependency on LoweredGaugeJet. The captured source SHA was checked; the committed dependency and unchanged candidate were independently recompiled with standard axiom audits. A recovery route with that declared dependency was dispatched, followed by two replacement consumers. Original failed/queued records are preserved; superseded-routes.json excludes those three old routes from effective counts. Recovery routes do not count as three additional mathematical goals.

## Boundaries
The chart/Ricci bridge explicitly preserves frame and curvature-sign conventions. Local coefficient identities and these estimates do not prove a quasilinear PDE solver. No actual flow, surgery/extinction trace, or smoothing theorem is claimed from interface assembly. V1 remains immutable.

## Runtime
Reuse the existing shared finite dispatcher. GPT-6 Luna only, high/xhigh/max, model concurrency unlimited, six compiler slots, two verifier slots, one Git writer. No full dependency rebuild, remote push or upstream PR.

## Known limits
Initial-metric proof passes pinned compilation but its default-resource elaboration still times out. A separate connection-differentiability portability repair is recorded in the operations directory only once actually committed. Preserve provenance and compare actual source SHA, rather than assuming the original task commit equals a later repaired source.
