import Mathlib.MeasureTheory.Constructions.HaarToSphere

-- Compatibility shim for the upstream Moreira port. The pinned mathlib already contains the
-- `MeasureTheory.Measure.toSphereBallBound` definition and its two sphere-ball estimates in
-- `Mathlib.MeasureTheory.Constructions.HaarToSphere`, so the old PR snapshot is not redeclared
-- here. The original upstream provenance is retained by this file and its import.
