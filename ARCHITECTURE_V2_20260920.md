# 无模型并发上限的统一证明调度架构

日期：2026-09-20。此次修改为调度基础设施，不是新的数学证明。

## 已部署

Luna 全局模型并发与默认派发预算均为 0（unlimited），取消原有 8/32 硬上限。所有新模型调用显式使用 gpt-5.6-luna，并只允许 high、xhigh、max。Grok 保持全局暂停，不会回退。

模型线程按就绪任务动态创建；依赖、额度暂停、目标路径冲突及可用内存仍是正确的准入条件。编译默认 6 槽、独立验证默认 2 槽、集成单写者，不把本机资源槽位解释成模型并发上限。旧编译锁与新资源锁通过同一 inode 共享，原有证明进程未重启。

固定陈述任务由原始模板重建证明体，经过源码编译与传递公理检查后集成，再解锁依赖。优先级之外考虑未完成下游关键链及可解锁任务。准备工作、模型运行、编译、验证和集成不再共用一个串行循环。

探索任务使用同一 SQLite 注册表记录，但保留独立状态：RUNNING、REVIEW_REQUIRED、VALIDATION_FAILED、APPROVED、INTEGRATED 等。已退出任务冻结源码和报告，不能因退出码 0 自动变成已证明。只有受信任目标清单的独立编译、传递公理检查、明确的陈述/定义审查，以及源码匹配真实提交后，才发布接受门。

## 现场运行

已幂等接管原 Luna 批次的 22 个任务；核对时 21 个已退出任务处于待审查队列，1 个原任务仍运行。没有重复启动旧模型。旧手工启动器的新任务入口已暂停，原进程继续。近期三个交卷的目标清单已接入自动独立验证，未自动批准数学语义。

统一监督进程已运行，PID 414002，项目 poincare-luna-unified-20260920。这是本机实际软件进程，不是聊天助手承诺稍后主动回复。新固定任务入队后依赖就绪即派发；多文件探索修改仍需要语义审查。该程序不会自动推送 GitHub。

## 验证

部署目录下 91 项单元测试通过。包含超过旧上限的任务派发、依赖解锁、重复领取和写入冲突、额度/档位检查、研究快照、审查门以及禁止默认全量重建。

另执行了零模型调用的真实 Lean/Git 测试：两个独立任务并行、汇合任务集成、接受门打开、后继任务集成；全部完成。真实研究验收中，合格源码经审查和提交核对后打开接受门，而间接依赖 sorryAx 的候选被拒绝。工程算术测试不计入数学成果。

可信依赖缓存由原始源码、模块名、递归依赖、工具链和编译选项形成内容键，校验完整 .olean/.private/.server 文件组合；不使用工人产出的旧对象文件。数据库、可信缓存不授予工人写权限，额外写权限仅包含实际工作目录、认证 CLI 自身目录和资源锁目录。

历史状态逐行核对保留：{"projects": 16, "tasks": 172, "attempts": 170}。本轮没有修改任何 .lean 数学源码，也没有改变 blueprint 完成标记。

## 仍然需要区分

无限模型并发不能消除真实数学依赖，不能保证完成时间最短。自动化覆盖固定目标的派发、编译验收、集成和解锁，以及探索工作的收卷和验证；它不自动判定新数学定义是否合适，也不保证从任意难题自动产生正确的完整拆分。新定义、陈述修正和原证明路线变更仍需审查。

## 操作入口

```bash
python3 tools/lean_swarm/status_report.py --project poincare-luna-unified-20260920
python3 tools/lean_swarm/cli.py enqueue poincare-luna-unified-20260920 /path/to/frozen-cards.json
python3 tools/lean_swarm/cli.py research-status poincare-luna-unified-20260920
```

监督进程使用显式 --watch；普通 run 仍为有限批次。停止监督器发送 SIGTERM；它不会重新启动或终止被接管的旧任务。完整命令和研究审查接口见 tools/lean_swarm/README.md。

源码、备份和本轮验证日志位于 /home/helenanana/projects/poincare-ops-20260919/architecture-v2-20260920-214733。认证、原始模型日志和本地配置未加入仓库。
